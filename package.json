{
  "name": "alertdc-bsky",
  "version": "1.0.0",
  "private": true,
  "description": "Bluesky bot that mirrors AlertDC alerts (excluding crime).",
  "scripts": {
    "start": "tsx src/node-entry.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@atproto/api": "^0.19.11",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
