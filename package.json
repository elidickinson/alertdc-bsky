{
  "name": "alertdc-bsky",
  "version": "1.0.0",
  "private": true,
  "description": "Bluesky bot that mirrors AlertDC alerts (excluding crime).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "node:start": "tsx src/node-entry.ts"
  },
  "dependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.95.0"
  }
}
